<%= form_for(@image.likes.find_by(user_id: current_user.id),
             html: { method: :delete },
             remote: true) do |f| %>
    <div>
    <%= @image.likes.count %> likes
    </div>
  <button class="btn btn-primary" type="submit">Unlike</button>
<% end %>